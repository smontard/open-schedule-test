name: CI

on:
  pull_request:
    branches: [main]

steps:
  - name: Apercu
    uses: dev-celer/apercu-action@v0.1.0
    env:
      NEON_PROJECT_ID: ${{ secrets.project_id }}
      NEON_API_KEY: ${{ secrets.api_key }}
    with:
      debug: true
